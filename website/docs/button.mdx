---
title: Button 按钮
---

import Button, { ButtonGroup } from '../../es/button/index';
import '../../es/button/index.css';
import { IconSearch } from '../../es/icon/index';

## 引入

```jsx
import { Button } from 'x-rc';

// 如果需要单独引入样式
import 'x-rc/es/button/index.css';
```

## 演示

### 按钮类型

支持 `default`、`primary`、`success`、`warning`、`error` 五种类型，默认是 `default`。

```jsx
<Button>Default</Button>
<Button type="primary">Primary</Button>
<Button type="success">Success</Button>
<Button type="warning">Warning</Button>
<Button type="error">Error</Button>
```

<Button>Default</Button>{' '}
<Button type="primary">Primary</Button>{' '}
<Button type="success">Success</Button>{' '}
<Button type="warning">Warning</Button>{' '}
<Button type="error">Error</Button>

### 扁平按钮

```jsx
<Button flat>Flat</Button>
<Button type="primary" flat>Flat</Button>
<Button type="success" flat>Flat</Button>
<Button type="warning" flat>Flat</Button>
<Button type="error" flat>Flat</Button>
```

<Button flat>Flat</Button>{' '}
<Button type="primary" flat>Flat</Button>{' '}
<Button type="success" flat>Flat</Button>{' '}
<Button type="warning" flat>Flat</Button>{' '}
<Button type="error" flat>Flat</Button>

### 文字按钮

```jsx
<Button text>Text</Button>
<Button type="primary" text>Text</Button>
<Button type="success" text>Text</Button>
<Button type="warning" text>Text</Button>
<Button type="error" text>Text</Button>
```

<Button text>Text</Button>{' '}
<Button type="primary" text>Text</Button>{' '}
<Button type="success" text>Text</Button>{' '}
<Button type="warning" text>Text</Button>{' '}
<Button type="error" text>Text</Button>

### 纯按钮

```jsx
<Button pure>Pure</Button>
<Button type="primary" pure>Pure</Button>
<Button type="success" pure>Pure</Button>
<Button type="warning" pure>Pure</Button>
<Button type="error" pure>Pure</Button>
```

<Button pure>Pure</Button>{' '}
<Button type="primary" pure>Pure</Button>{' '}
<Button type="success" pure>Pure</Button>{' '}
<Button type="warning" pure>Pure</Button>{' '}
<Button type="error" pure>Pure</Button>

### 半圆

```jsx
<Button type="primary" round>
  Round
</Button>
```

<Button type="primary" round>
  Round
</Button>

### 全圆形

```jsx
<Button type="primary" fullRound>
  <IconSearch />
</Button>
```

<Button type="primary" fullRound>
  <IconSearch />
</Button>

### 链接按钮

```jsx {2-3}
<Button
  href="http://t.tt"
  target="_blank"
  type="primary"
>
  Smartisan
</Button>
```

<Button href="http://t.tt" target="_blank" type="primary">
  t.tt
</Button>

### 块级按钮

```jsx
<Button block>This is block.</Button>
```

<Button block>This is block.</Button>

### 尺寸

支持 `large`、`normal`、`small`，默认是 `normal`。

```jsx
<Button size="large">Large</Button>
<Button size="normal">Normal</Button>
<Button size="small">Small</Button>
```

<Button size="large">Large</Button>{' '}
<Button size="normal">Normal</Button>{' '}
<Button size="small">Small</Button>

### 禁用

```jsx
<Button disabled>Disabled</Button>
<Button type="primary" disabled>Disabled</Button>
<Button flat type="error" disabled>Disabled</Button>
<Button text disabled>Disabled</Button>
```

<Button disabled>Disabled</Button>{' '}
<Button type="primary" disabled>Disabled</Button>{' '}
<Button type="error" flat disabled>Disabled</Button>{' '}
<Button type="primary" text disabled>Disabled</Button>

### 按钮组

```jsx
import { ButtonGroup } from 'x-rc';
```

```jsx
<ButtonGroup>
  <Button>Past</Button>
  <Button>Present</Button>
  <Button>Future</Button>
</ButtonGroup>
<ButtonGroup type="primary">
  <Button>Past</Button>
  <Button>Present</Button>
  <Button>Future</Button>
</ButtonGroup>
```

<ButtonGroup>
  <Button>Past</Button>
  <Button>Present</Button>
  <Button>Future</Button>
</ButtonGroup>{' '}
<ButtonGroup type="primary">
  <Button>Past</Button>
  <Button>Present</Button>
  <Button>Future</Button>
</ButtonGroup>

## API

### Button Props

| 参数 | 类型 | 默认值 | 说明 |
|-----|------|-------|-----|
| type | `default`, `primary`, `success`, `warning`, `error` | `default` | 基本类型 |
| size | `normal`, `large`, `small` | `normal` | 尺寸 |
| flat | `boolean` | `false` | 扁平按钮 |
| text | `boolean` | `false` | 文字按钮 |
| pure | `boolean` | `false` | 纯按钮 |
| round | `boolean` | `false` | 半圆 |
| fullRound | `boolean` | `false` | 全圆 |
| block | `booleab` | `false` | 块级按钮 |
| disabled | `boolean` | `false` | 禁用 |
| href | `string` | - | 链接 |
| target | `_self`, `_blank` | `_blank` | 链接的打开方式 |
| onClick | `React.EventHandler` | - | 点击回调 |
| className | `string` | - | 类名 |
| style | `React.CSSProperties` | - | 样式 |

### ButtonGroup Props

| 参数 | 类型 | 默认值 | 说明 |
|-----|------|-------|-----|
| children | `React.ReactElement[]` | - | 内嵌 Button 组件 |
| className | `string` | - | 类名 |
| style | `React.CSSProperties` | - | 样式 |
