---
title: Loader 加载
---

import Loader from '../../es/loader/index';
import '../../es/loader/index.css';

## 引入

```jsx
import { Loader } from 'redefy';
```

## 演示


### 简单示例
```jsx
export function Demo1() {
  return (
    <Loader />
  );
}
```

export function Demo1() {
  return (
    <Loader />
  );
}

<Demo1 />

### 带文字

```jsx
export function Demo2() {
  return (
    <Loader label="Loading" />
  );
}
```

export function Demo2() {
  return (
    <Loader label="Loading" />
  );
}

<Demo2 />

<br /><br />

垂直排列：`vertical: boolean`

```jsx {5}
export function Demo3() {
  return (
    <Loader
      label="加载中"
      vertical
    />
  );
}
```

export function Demo3() {
  return (
    <Loader
      label="加载中"
      vertical
    />
  );
}

<Demo3 />

## API

### Props

| 参数 | 类型 | 默认值 | 说明 |
|-----|------|-------|-----|
| color | `string` | `currentColor` | 颜色 |
| colorType | `default`, `primary`, `success`, `warning`, `error` | `default` | 内置的颜色类型 |
| size | `string`, `number` | `1em` | 尺寸 |
| label | `React.ReactNode` | - | 标签 |
| vertical | `boolean` | `false` | 是否垂直排列 |
| className | `string` | - | 类名 |
| style | `React.CSSProperties` | - | 样式 |
