import { show, info, success, warning, error } from './show';

export * from './Toast';

export { show, info, success, warning, error };

export default {
  show,
  info,
  success,
  warning,
  error,
};
